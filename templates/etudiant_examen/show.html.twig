{% extends 'base2.html.twig' %}

{% block title %}EtudiantExamen{% endblock %}

{% block body %}
    <h1>EtudiantExamen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant_examan.id }}</td>
            </tr>
            <tr>
                <th>NumeroEtudiant</th>
                <td>{{ etudiant_examan.numeroEtudiant }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ etudiant_examan.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudiant_examen_index') }}">back to list</a>

    <a href="{{ path('app_etudiant_examen_edit', {'id': etudiant_examan.id}) }}">edit</a>

    {{ include('etudiant_examen/_delete_form.html.twig') }}
{% endblock %}
